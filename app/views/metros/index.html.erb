<p id="notice"><%= notice %></p>

<h1>Metrôs</h1>

<table>
  <thead>
    <tr>
      <th>Linha metro</th>
      <th>Empresa</th>
      <th>Qtd passageiros</th>
      <th>Data inicio operacao</th>
      <th>Ultima revisao</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @metros.each do |metro| %>
      <tr>
        <td><%= metro.linha_metro.id %></td>
        <td><%= metro.empresa.id %></td>
        <td><%= metro.qtd_passageiros %></td>
        <td><%= metro.data_inicio_operacao %></td>
        <td><%= metro.ultima_revisao %></td>
        <td><%= link_to 'Show', metro %></td>
        <td><%= link_to 'Edit', edit_metro_path(metro) %></td>
        <td><%= link_to 'Destroy', metro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Metro', new_metro_path %>
<%= link_to 'Back', welcome_index_path %>
