<%= form_with(model: linha_metro, local: true) do |form| %>
  <% if linha_metro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(linha_metro.errors.count, "error") %> prohibited this linha_metro from being saved:</h2>

      <ul>
      <% linha_metro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numero_paradas %>
    <%= form.number_field :numero_paradas, id: :linha_metro_numero_paradas %>
  </div>

  <div class="field">
    <%= form.label :cor %>
    <%= form.text_field :cor, id: :linha_metro_cor %>
  </div>

  <div class="field">
    <%= form.label :passageiros_atendidos %>
    <%= form.text_field :passageiros_atendidos, id: :linha_metro_passageiros_atendidos %>
  </div>

  <div class="field">
    <%= form.label :qtd_trans %>
    <%= form.text_field :qtd_trans, id: :linha_metro_qtd_trans %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
